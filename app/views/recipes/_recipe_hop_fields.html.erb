<fieldset>
  <div class="field">
    <%= f.select :hop_id, Hop.all.collect {|c| [c.name, c.id]} %>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :time %>
    <%= f.text_field :time %>
  </div>
    <%= link_to raw('<i class="icon-remove"></i>'), "#", :class => 'remove_fields' %>
</fieldset>
