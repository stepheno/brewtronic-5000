<tr>
  <td> <%= f.text_field :mineral_virt_attr, :class => 'recipe_autocomplete', :data => {:source => '/minerals.json' }  %> </td>
  <%= f.hidden_field :mineral_id %> 
  <td> <%= f.text_field :amount %> </td>
  <td> <%= f.select :unit, options_for_select(["kg", "lb", "oz", "g"], :selected => current_user.mass_units) %> </td>
  <td> <%= link_to raw('<i class="icon-remove"></i>'), "#", :class => 'remove_fields' %> </td>
</tr>
