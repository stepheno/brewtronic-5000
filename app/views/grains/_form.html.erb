<%= form_for(@grain) do |f| %>
  <% if @grain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grain.errors.count, "error") %> prohibited this grain from being saved:</h2>

      <ul>
      <% @grain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :extract_potential %><br />
    <%= f.text_field :extract_potential %>
  </div>
  <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
