<h1>Grain Inventory</h1>
<div class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <div class="nav-collapse">
        <%= form_tag grain_inventories_path, :method => 'get', :id => "grain_inventories_search", :class => "navbar-search pull-left" do %>
          <%= text_field_tag :search, params[:search], :class => "search-query", :placeholder => "Search" %>
        <% end %>
        <%= link_to raw('<i class="icon-plus-sign icon-white"></i> New Grain Inventory'), new_grain_inventory_path, :class => 'btn btn-primary pull-right'%>
      </div>
    </div>
  </div>
  <div id="grain_inventories"><%= render 'grain_inventories' %></div>
</div>
