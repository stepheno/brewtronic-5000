.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", grain_inventories_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_grain_inventory_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      GrainInventory
    .inner
      = form_for :grain_inventory, :url => grain_inventories_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
