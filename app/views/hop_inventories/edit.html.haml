.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", hop_inventories_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_hop_inventory_path
      %li.active= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_hop_inventory_path
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      HopInventory
    .inner
      = form_for @hop_inventory, :url => hop_inventory_path(@hop_inventory), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
