<h1>Mash Types</h1>
<div class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <div class="nav-collapse">
        <%= form_tag mash_types_path, :method => 'get', :id => "mash_types_search", :class => "navbar-search pull-left" do %>
          <%= text_field_tag :search, params[:search], :class => "search-query", :placeholder => "Search" %>
        <% end %>
        <%= link_to raw('<i class="icon-plus-sign icon-white"></i> New Mash Type'), new_mash_type_path, :class => 'btn btn-primary pull-right'%>
      </div>
    </div>
  </div>
  <div id="mash_types"><%= render 'mash_types' %></div>
</div>
